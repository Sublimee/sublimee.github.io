# magusev.ru

## Tests

- `npm test` or `npm run test:e2e` runs all Playwright tests.
- `npm run test:e2e:ui` runs tests with Playwright UI.
- `npm test -- --update-snapshots`  runs tests with updates sample after intentional UI changes.